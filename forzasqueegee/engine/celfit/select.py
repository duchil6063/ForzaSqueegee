"""획 선별 문턱 — 이 획이 **무엇을 가르고 있나**를 양옆에서 읽은 값의 자.

값 자체는 `evidence`가 획마다 양옆(폭/2+여유 법선) 표본에서 낸다
(`StrokeEvidence.bnd`·`sil`). 획 위가 아니라 양옆에서 읽는 것이 요점이다:
선 위는 무엇을 재든 "대비가 세다"로 나온다. 여기 두는 것은 그 값을 역할로
가르는 문턱뿐이고, 역할 판정(`graph.classify`)이 이 자를 쓴다.
"""

from __future__ import annotations

_THIN_BND = 0.30       # 경계성(양옆 영역이 다른 표본 비율)이 이 이상이면 윤곽 — 보호
_THIN_SIL = 0.10       # 한쪽이 배경(-1)인 표본이 이 비율만 넘어도 실루엣 윤곽 — 보호
