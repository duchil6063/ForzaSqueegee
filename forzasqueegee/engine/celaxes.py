"""cel 노선의 **단계 스위치** — 축을 하나씩 끄고 켜서 무엇이 무엇을 했는지 가른다.

전부 기본 켬이고, 끄면 그 축이 들어오기 전의 동작으로 돌아간다. 회귀 계측이
이 스위치로 판을 여러 벌 구워 나란히 세운다. 어느 축이 어떤 오차를 줄였는지는 그 표가 답한다.

    FS_CEL_INKFILL   §1  선 제거를 면 귀속 복원으로 (끄면 유클리드 최근접)
    FS_CEL_OVERSEG   §2  원자 그리드 재분할 (끄면 watershed 원자 그대로)
    FS_CEL_RAG       §3  MDL 그래프 병합 (끄면 문턱 사다리 — `celart.legacy`)
    FS_CEL_DENSE     §4  밀집 시각 특징을 병합 보조 증거로 (모델 있을 때만)
    FS_CEL_TAIL      §5  팔레트 꼬리 센터 보강
    FS_CEL_DESCFIT   §6  서술자 후보 순위 — **기본 꺼짐** (아래 근거)
    FS_CEL_LAYERED   §7  영역 전체를 덮는 바탕 도형 먼저 (끄면 봉우리 탐욕)
    FS_CEL_SETCOVER  §8  후보 집합 덮개 · 작은 빔 탐색
    FS_CEL_PAIR      §9  이웃 영역 공유 경계 (이음 당김)
    FS_CEL_SNAP_GEO  §10 획 스냅을 측지로 (끄면 유클리드 최근접)
    FS_CEL_RESIDUAL  §12 잔차를 **기존 도형을 움직여** 먼저 고친다
    FS_CEL_GROWFIRST §14 잔여를 **사기 전에** 기존 도형을 늘려 먹는다
    FS_CEL_MERGEJND  §15 병합 비용의 색차에 **JND 하한**을 둔다
    FS_CEL_POSTPRICE §16 다 그린 판에서 값을 다시 물어 못 미치는 장을 되판다
    FS_CEL_SILCAP    §17 실루엣 밖 스필을 값이 아니라 **자격**으로 막는다
    FS_CEL_SEAL      §18 실루엣 안 미커버 표본을 **0으로 봉인**한다 (λ와 거래 없음)
    FS_CEL_RAMP      §19 비탈을 자른 경계는 병합의 색 손해를 깎는다 (음영 추상)
    FS_CEL_OVERPAINT §20 물림 밴드를 앞뒤 안 가리고 당긴다 — 틈 대신 겹침

`FS_CEL_NEW=0` 한 방이면 전부 끈 옛 동작이다 (a0 대조판).

**§6만 기본이 꺼져 있다.** 서술자 순위 자체는 제 일을 한다 — 잔여를 닮은
도형이 앞에 서고, 굽은 껍질에 초승달이 한 장으로 선다. 다만 그 순위가 여는
넓은 어휘(불투명·뚱뚱한 76종)의 값이 회귀 열 장에서 안 나왔다: 최종 설정에서
이 축만 빼 보면(대조판 b1-nodesc) 총 도형이 1,984 → 1,997로
**늘어나는 대신** 선/색면 틈이 580 → 659px, 중요도 가중 오차가 15.09 → 15.28로
나빠진다. 어휘가 넓어지면 가장자리가 너덜한 도형이 잔여를 잘 주워 점수로
이기지만 육안이 나빠진다는 종전 관찰(`celfit.vocabulary` 문서)이 순위를 얹어도
남는다는 뜻이다. 기계는 그대로 두고 스위치만 꺼 둔다 — 어휘가 바뀌거나
카탈로그가 늘면 다시 재 볼 자리다 (`FS_CEL_DESCFIT=1`).

## §18~§20이 무엇을 했나 (표준 11장, 기준판 = 셋 다 끔)

셋은 한 벌이다. **§20**이 틈을 안 생기게 하고(경계의 주인을 정해 겹쳐 칠한다),
**§18**이 그러고도 남은 미커버를 자격으로 닫고, **§19**가 음영을 자른 가짜
경계를 병합해 영역을 줄인다. §18을 혼자 켜면 값이 비싸다 — 틈을 그대로 두고
메우기만 하면 봉인이 도형을 판당 200장 넘게 산다.

| | 기준판 | 셋 다 (배포 기본) |
|---|---|---|
| 실루엣 안 미커버 표본 (2배 슈퍼샘플) | 3,000~9,000 | **0** (11장 전부) |
| 선/색면 틈 px | 687 | **355** (−48%) |
| 작은 중요 영역 보존 | 0.876 | **0.925** (+5.6%) |
| 반대색 슬리버 | 955 | **937** (−1.9%) |
| 면 안 틀림 (`wrong_far_rate`) | 0.00488 | **0.00476** (−2.5%) |
| 보이는 오차 (`imp_error_seen`) | 0.5632 | 0.5634 (±0) |
| 영역 | 496 | **474** (−4.4%) |
| 논리 획 | 531 | **512** (−3.6%) |
| 채움 갈아타기 | 0.765 | **0.624** (−18.5%) |
| 총 도형 | 1,865 | 1,995 (+7.0%) |
| 런타임 | 502s | **349s** (−30%) |

**총 도형이 느는 것은 전부 봉인 값이다.** 11장 합계로 22,988장 중 봉인이
1,694장이고, 그것을 빼면 21,958 → 21,294장(**−3.0%**)이다. 즉 구조 쪽은
줄었고, 늘어난 몫은 "실루엣 안에 안 칠한 표본이 하나도 없다"를 사는 값이다.

봉인 값을 더 깎으려고 잰 갈래는 전부 무효였다 (실측):

- **색으로 묶기** — 한 장은 색이 하나이니 같은 색끼리 묶는 편이 옳아 보이나,
  조각이 색 경계를 따라 흩어져 있어 군집이 색 수만큼 쪼개진다 (158 → 408개).
- **더 멀리 묶기** (반경 4 → 8px) — 군집은 26% 주는데 한 장으로 못 덮는
  잔여가 늘어 봉인 점이 그만큼 는다 (총 도형 ±0).
- **밑에 바탕 깔기** — 위에서 점을 찍는 대신 큰 도형을 맨 밑에 깔면 스필이
  공짜라 크게 잡을 수 있다. 그런데 구멍 색이 548가지(JND로 묶어도 75가지)라
  색 수가 곧 장수의 바닥이고, 반경 48px로 묶어도 226개 — 지금과 같다.
- **2→1 표현 다시 쓰기** — 연속 동색 겹침 쌍 206개 중 한 장으로 대체 가능한
  것이 안전한 허용오차에서 20개(플랜의 0.9%), 눈에 띄게 나빠지는 허용오차까지
  풀어도 129개(5.8%)다.

남은 미커버의 정체는 실측으로 이렇다 (01 기준, 봉인 전 7,115표본·378군집):
군집의 60%가 1px 이하이고 그 79.5%가 **잉크에서 1px 안**이며, 그중 57.7%는
잉크 폴리곤을 반올림한 래스터 **안**이면서 실제 벡터 **밖**이다. 넓이의 66%는
반대로 큰 군집 둘(안 그려진 영역)이 차지한다.

## §15~§17이 무엇을 했나 (표준 11장, 기준판 = 셋 다 끔)

셋은 한 벌이다 — §15가 장수를 걷어 내고, §17이 그 대가로 생기는 실루엣 밖
스필을 막고, §16이 남은 부스러기를 되판다. 축별 실측 중앙값:

| | 채움 도형 | 실루엣 밖 스필 | 원화 대비 RMSE |
|---|---|---|---|
| §15만 | −16.9% | +22% | +1.4% |
| §17만 | +10.0% | **−74%** | −3.3% |
| 셋 다 (배포 기본) | **−13.5%** | **−45.6%** | ±0.0% |

셋을 켠 판은 총 도형 2,017 → 1,865(−7.5%) · 한 장으로 끝난 영역 +5.1% ·
가려져 갈라진 짝 −24% · 작은 중요 영역 보존 +3.0%이고, 게이트는 11장 전부
통과한다. **선 도안은 한 칸도 안 바뀐다** (`line` 노선 3장 바이트 불변 확인).
"""

from __future__ import annotations

import os

_AXES = ("INKFILL", "OVERSEG", "RAG", "DENSE", "TAIL", "DESCFIT", "LAYERED",
         "SETCOVER", "PAIR", "SNAP_GEO", "RESIDUAL", "GROWFIRST", "MERGEJND",
         "POSTPRICE", "SILCAP", "SEAL", "RAMP", "OVERPAINT")


# 기본이 꺼진 축 — 근거는 위 문서 (회귀 열 장의 축 빼 보기 실측)
_OFF = ("DESCFIT",)


def on(axis: str) -> bool:
    """이 축이 켜져 있나 — `FS_CEL_NEW=0`이면 개별 스위치와 무관하게 전부 꺼짐."""
    if os.environ.get("FS_CEL_NEW", "1") == "0":
        return False
    return os.environ.get("FS_CEL_" + axis,
                          "0" if axis in _OFF else "1") != "0"


def names() -> dict:
    """지금 켜진 축 — report에 실어 판을 되짚는다."""
    return {a.lower(): on(a) for a in _AXES}
